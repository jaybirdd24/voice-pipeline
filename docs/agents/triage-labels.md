# Triage Labels

The skills speak in terms of five canonical triage roles. This file maps those roles to the actual label strings used in this repo's issue tracker.

| Label in mattpocock/skills | Label in our tracker | Meaning                                  |
| --------------------------- | --------------------- | ----------------------------------------- |
| `needs-triage`               | `needs-triage`        | Maintainer needs to evaluate this issue   |
| `needs-info`                 | `needs-info`          | Waiting on reporter for more information  |
| `ready-for-agent`            | `ready-for-agent`     | Fully specified, ready for an AFK agent   |
| `ready-for-human`            | `ready-for-human`     | Requires human implementation             |
| `wontfix`                    | `wontfix`             | Will not be actioned (GitHub default label, reused as-is) |

Plus two category labels (not part of the canonical five, but required alongside a state label):

| Category      | Label in our tracker | Meaning                        |
| -------------- | --------------------- | ------------------------------- |
| bug            | `bug`                 | Something is broken (GitHub default) |
| enhancement    | `enhancement`         | New feature or improvement (GitHub default) |

Using default strings throughout — no repo-specific renaming needed for a solo project.
